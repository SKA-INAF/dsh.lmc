# ----------------------------------------------------------------------------
# STEPS:
# ----------------------------------------------------------------------------
from behave import given, when, then
import nose
from tango import DeviceProxy
import PyTango

@given('SPF is in STANDBY-LP mode')
def step_impl(context):
	pass
